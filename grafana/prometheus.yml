global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'catalyst-exchange'
    static_configs:
      - targets: ['host.docker.internal:8000']
        labels:
          service: 'exchange'
          environment: 'local'
